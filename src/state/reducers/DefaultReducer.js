import * as Types from '../actions/types';

const INITIAL_STATE = {
    text: ''
};

/*
    An `action` object should contain two properties: 
    - type - action's type
    - payload - action's payload
*/

export default (state = INITIAL_STATE, action) => {
    return state;
};